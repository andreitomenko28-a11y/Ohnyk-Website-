# --- Server ---
PORT=4000

# Admin bearer token protecting GET /api/waitlist (CSV export).
# Generate a long random string, e.g.  openssl rand -hex 32
ADMIN_TOKEN=change-me-to-a-long-random-string

# CORS: the exact origin(s) of the deployed landing page.
# Comma-separated for multiple. Leave empty to allow any origin (dev only).
ALLOWED_ORIGIN=http://localhost:5500

# Max POST /api/waitlist submissions per IP per 15 min window.
RATE_LIMIT_MAX=5

# --- Database (SQLite) ---
# File path relative to the backend root (Prisma resolves it from prisma/).
DATABASE_URL=file:../data/waitlist.db

# --- SMTP (owner notification email) ---
# Any SMTP provider works. Recommended for production: Brevo (see README).
#
# Brevo (https://brevo.com  →  SMTP & API  →  SMTP):
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
# Brevo SMTP login — shown on the SMTP page, looks like 8xxxxx@smtp-brevo.com
SMTP_USER=xxxxxxxx@smtp-brevo.com
# Brevo SMTP key (NOT your account password) — generated on the SMTP page.
SMTP_PASS=your-brevo-smtp-key
# From address — MUST be a verified sender/domain in Brevo.
SMTP_FROM=Ohnyk Waitlist <no-reply@yourdomain.com>
# Where new-signup notifications are delivered.
NOTIFY_EMAIL=owner@example.com
#
# Alternative — Gmail SMTP (use a 16-char App Password, not your login password):
#   SMTP_HOST=smtp.gmail.com
#   SMTP_PORT=587
#   SMTP_USER=your-address@gmail.com
#   SMTP_PASS=your-app-password
#   SMTP_FROM=Ohnyk Waitlist <your-address@gmail.com>
