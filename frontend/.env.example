# Override the API base URL (default uses the Vite dev proxy at /api).
# VITE_API_URL="http://localhost:4000/api"

# Where the Vite dev proxy forwards /api requests.
# VITE_API_TARGET="http://localhost:4000"
